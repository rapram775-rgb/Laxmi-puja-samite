/** Shared Earth transform constants so the surface network spins in sync. */
export const EARTH_RADIUS = 1;
export const EARTH_TILT = 0.41; // ~23.5° axial tilt
export const EARTH_Y0 = 2.6; // initial spin offset (frames the Americas)
export const EARTH_SPIN = 0.06; // rad/s — slow, cinematic
